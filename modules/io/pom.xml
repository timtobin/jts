<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.locationtech.jts</groupId>
        <artifactId>jts-modules</artifactId>
        <version>1.20.1-SNAPSHOT</version>
    </parent>
    <artifactId>jts-io</artifactId>
    <name>${project.groupId}:${project.artifactId}</name>
    <packaging>pom</packaging>
    
    <profiles>
      <profile>
        <id>oracle</id>
        <activation>
           <property><name>all</name></property>
         </activation>
        <modules>
          <module>ora</module>
        </modules>
      </profile>
      <!--profile>
        <id>arcsde</id>
        <activation>
           <property><name>all</name></property>
         </activation>
        <modules>
          <module>sde</module>
        </modules>
      </profile-->
    </profiles>
    
    <dependencies>
      <dependency>
          <groupId>org.locationtech.jts</groupId>
          <artifactId>jts-core</artifactId>
          <version>${project.version}</version>
      </dependency>
    </dependencies>

    <modules>
        <module>common</module>
    </modules>
    
    <!-- add inside <project><build><plugins> ... </plugins></build> of the root POM -->
    <build>
      <plugins>
        <plugin>
          <groupId>com.diffplug.spotless</groupId>
          <artifactId>spotless-maven-plugin</artifactId>
          <version>2.43.0</version>

          <configuration>
            <!-- normalize line endings across OSes -->
            <lineEndings>UNIX</lineEndings>

            <!-- Java sources -->
            <java>
              <!-- format with google-java-format -->
              <googleJavaFormat>
                <version>1.23.0</version>
                <style>GOOGLE</style>
              </googleJavaFormat>
              <!-- tidy imports after formatting -->
              <removeUnusedImports/>
              <importOrder>java,javax,org,com</importOrder>

              <!-- which files to format -->
              <includes>
                <include>src/**/java/**/*.java</include>
              </includes>
              <!-- example excludes (uncomment if needed)
              <excludes>
                <exclude>**/generated-sources/**</exclude>
              </excludes>
              -->
            </java>

            <!-- format POMs consistently -->
            <pom>
              <includes>
                <include>pom.xml</include>
                <include>**/pom.xml</include>
              </includes>
            </pom>

            <!-- Optional: apply a license header to Java files
            <java>
              <licenseHeaderFile>${project.root}/license-header.txt</licenseHeaderFile>
              ...
            </java>
            -->
          </configuration>

          <executions>
            <!-- fail the build if formatting is off -->
            <execution>
              <id>spotless-check</id>
              <goals>
                <goal>check</goal>
              </goals>
              <phase>verify</phase>
            </execution>
            <!-- no binding for apply; run it manually when needed -->
          </executions>
        </plugin>
      </plugins>
    </build>
</project>